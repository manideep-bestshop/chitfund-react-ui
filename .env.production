# Used for production builds (npm run build).
# This file IS committed to git — no secrets.
# The CI/CD pipeline overrides REACT_APP_API_BASE_URL at build time per environment.
# Update this once the prod Container App is provisioned.
REACT_APP_API_BASE_URL=https://ca-chitfund-prod-api.placeholder.azurecontainerapps.io
REACT_APP_ENV=production
